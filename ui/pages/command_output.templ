package pages

templ LaunchCommandInTerminal(id, title, script string) {
  <div class="mt-2">
    <div class="text-sm text-gray-300 mb-1">{ title }</div>
    <div id={"inline-term-" + id} data-inline-term={id} class="rounded" style="height:10rem;background:#000;"></div>
  </div>
}

